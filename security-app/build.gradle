repositories {
    mavenCentral()
}

dependencies {
    implementation project(':security-api')
    implementation project(':library-app')
    implementation project(':common')
}

test {
    useJUnitPlatform()
}
project(":security-app") {
    sonar {
        skipProject = true
    }
}