repositories {
    mavenCentral()
}

dependencies {
    implementation (project(':library-api')){
        transitive = true
    }
    implementation(project(':common')) {
        transitive = true
    }
}