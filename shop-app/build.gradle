dependencies {
    implementation project(':shop-api')
    implementation(project(':library-app')) {
        transitive = true
    }
    implementation(project(':common')) {
        transitive = true
    }
}

tasks.named('test') {
    useJUnitPlatform()
}
