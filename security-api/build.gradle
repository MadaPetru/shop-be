repositories {
    mavenCentral()
}

dependencies {
    implementation project(':library-api')
    implementation project(':common')
}
project(":security-api") {
    sonar {
        skipProject = true
    }
}